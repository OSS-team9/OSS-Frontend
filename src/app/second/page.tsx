export default function SecondPage() {
  return (
    // <main> 태그 등은 layout.tsx에 있으므로,
    // 여기서는 실제 '내용'만 만들면 됩니다.
    // 모바일 화면을 가정하고 패딩(여백)을 줍니다.
    <div className="p-4">
      <h1 className="text-2xl font-bold">두번째 페이지</h1>
    
    </div>
  );
}